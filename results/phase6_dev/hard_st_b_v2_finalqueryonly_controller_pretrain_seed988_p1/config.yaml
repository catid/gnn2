_base_: ./hard_st_benchmark_b_v2_router2_controlleronly_imitation_release.yaml

experiment:
  seed: 988

benchmark:
  mode_probs: [0.0, 0.0, 1.0]

routing:
  force_oracle_actions_until_step: 96
  oracle_route_weight_start: 0.5
  oracle_route_weight_end: 0.1
  oracle_route_weight_schedule_steps: 160

training:
  train_steps_delta: 320
  val_every: 20
