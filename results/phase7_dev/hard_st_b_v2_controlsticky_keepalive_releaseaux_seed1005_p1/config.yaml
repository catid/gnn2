_base_: ./hard_st_benchmark_b_v2_controlsticky_keepalive_finalquerywaitonly.yaml

experiment:
  seed: 1005

model:
  release_scale: 1.0
  release_gate_mode: bias
  release_gate_scale: 1.5

routing:
  release_loss_weight_start: 0.35
  release_loss_weight_end: 0.08
  release_loss_weight_schedule_steps: 320
  release_target_scope: final_query_only
  release_positive_weight: 4.0

training:
  selection_metric: per_mode.delay_to_final_query.route_match
