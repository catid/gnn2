_base_: ./hard_st_benchmark_b_v2_controlsticky_keepalive_longerstrong.yaml

experiment:
  seed: 1701

teacher:
  run_dir: results/phase7_dev/hybrid_es_b_v2_forceoracle_longerstrong_resume_from1305_seed1605_p1
  route_mode: hard
  estimator: straight_through
  distill_temperature: 1.0
  target_scope: final_query_only
  logits_weight: 0.15
  route_weight: 0.75
  control_prob_weight: 0.25
  release_prob_weight: 0.15

training:
  train_steps_delta: 512
  val_every: 24
