_base_: ../dev/hard_st_benchmark_b_v2_gatedblend_maskcurr.yaml

experiment:
  seed: 430

routing:
  exit_mask_final_query_only: true
  exit_mask_trigger_exit_until_trigger: true
  oracle_route_weight: 0.2
  delay_write_weight: 0.25
