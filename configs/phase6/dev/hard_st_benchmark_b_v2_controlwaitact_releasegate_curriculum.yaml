_base_: ./hard_st_benchmark_b_v2_controlwaitact_setclear_curriculum.yaml

experiment:
  seed: 983

model:
  release_gate_mode: direct
  release_gate_scale: 2.0
  release_scale: 0.0

routing:
  release_loss_weight_start: 0.5
  release_loss_weight_end: 0.1
  release_loss_weight_schedule_steps: 240
  release_target_scope: final_query_only
  release_positive_weight: 4.0

training:
  train_steps: 320
  val_every: 20
