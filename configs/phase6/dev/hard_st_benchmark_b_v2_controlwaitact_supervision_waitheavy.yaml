_base_: ./hard_st_benchmark_b_v2_controlwaitact_setclear_curriculum.yaml

experiment:
  seed: 991

routing:
  wait_loss_weight: 0.5
  wait_target_scope: oracle_delayed_only
  control_state_weight_start: 0.75
  control_state_weight_end: 0.2
  control_state_weight_schedule_steps: 240

training:
  train_steps_delta: 320
  val_every: 20
