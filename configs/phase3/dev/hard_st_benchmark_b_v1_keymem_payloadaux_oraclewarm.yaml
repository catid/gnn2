_base_: ./hard_st_benchmark_b_v1_keymem_writeaux_oraclewarm.yaml

experiment:
  seed: 705

model:
  packet_memory_write_bias: -0.5

routing:
  force_oracle_actions: true
  oracle_route_weight: 0.5
  delay_write_weight: 0.5
  memory_payload_weight: 0.75
