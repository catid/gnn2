_base_: ./hard_st_benchmark_b_v2_keymem_writeaux_maskcurr.yaml

experiment:
  seed: 706

model:
  packet_memory_write_bias: -0.5

routing:
  exit_mask_final_query_only: true
  exit_mask_trigger_exit_until_trigger: true
  oracle_route_weight: 0.2
  delay_write_weight: 0.5
  memory_payload_weight: 0.75
