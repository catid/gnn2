_base_: ../base/common.yaml

experiment:
  seed: 305

benchmark:
  name: long_horizon_memory
  num_nodes: 4
  obs_dim: 24
  num_classes: 4
  seq_len: 128

model:
  hidden_dim: 128
  max_internal_steps: 4
  adapter_rank: 8

objective:
  lambda_hops: 0.01
  lambda_delay: 0.0025
  lambda_ttl: 0.5

training:
  batch_size: 256
  val_batch_size: 512
  train_steps: 220
  val_every: 40
  truncate_bptt_steps: 32
  val_batches: 12
  test_batches: 16

method:
  name: soft
  temperature: 1.0
