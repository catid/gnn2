_base_: ../base/common.yaml

experiment:
  seed: 301

benchmark:
  name: mixed_oracle_routing
  num_nodes: 8
  obs_dim: 40
  seq_len: 2

model:
  hidden_dim: 128
  max_internal_steps: 8
  adapter_rank: 8

training:
  batch_size: 512
  val_batch_size: 1024
  train_steps: 300
  val_every: 50
  val_batches: 16
  test_batches: 24

method:
  name: soft
  temperature: 1.0
