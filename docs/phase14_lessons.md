# Phase 14 Lessons

## Status

Complete.

## Working Notes

- Dual-anchor route/content contracts can preserve the stabilized late-route regime very well without reopening shortcut collapse, but that is not enough by itself to move held-confirm content.
- The first fully paneled dual-anchor family (`17031`) is useful because it removes one ambiguity cleanly: branch stability and held-confirm content lift are now separable again in phase 14, just as shortcut stability and held-confirm lift were separable in phase 13.
- The first fully paneled teacher-first content-only family (`17024`) now says the same thing from the mainline lane: content-branch-only supervision preserves the late-route regime and wins the content-failure hard slice, but still does not convert those summary-time gains into held-confirm recovery.
- The stronger teacher-first follow-up (`17025`) closes the same way, so simple teacher-only scaling is no longer a live frontier by itself.
- The first credible gated sidecar branch (`17063`) also closes the same way, which is useful because it removes another ambiguity: the remaining ceiling does not look like it disappears just by adding a tiny content-only path that cannot touch routing.
- The hard-slice lane also closes the same way once fully verified: summary-time gains are real, but locked confirm still falls back to the same held-confirm regime.
- The bounded `1821` sanity check (`17101`) is useful because it shows the best phase-14 recipe is not completely source-specific, but the carryover is weak and confirm-time metrics still collapse back to the old medium-source regime.
- The paired `1879` sanity checks (`17103`, `17104`) matter because they remove the last easy alternative explanation: these selectors do not manufacture false-positive content recovery on a bad source.
- The remaining honest question is now narrower than the broad cluster list suggests: the current single-slot content path seems supervision-limited only up to a point, and the next justified move looks more like a deeper content-path architectural change than more label/teacher shaping on the same branch.
